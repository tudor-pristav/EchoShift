package echoshift.services;

public class ShopService {

    /**
     * Attempts to purchase an item.
     * @return true if purchase succeeded
     */
    public boolean purchaseItem(String userId, String itemName, int price) {
        // TODO: Implement real logic (check balance, update playerData JSON, etc.)
        // For now, always return true for testing
        return true;
    }
}